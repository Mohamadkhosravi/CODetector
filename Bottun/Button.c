#include "Button.h"
void buttonInit(void){
	
	
	
}
/*
void buttonUpdate(void){
 char buttonState = _pa4;
  int currentTime=0; 
   if (buttonState == SET) {
   	
        debounceCounter++;
        currentTime++;
        
    }
	else {
	    debounceCounter = 0;
	}
	
	 if (debounceCounter >= DEBOUNCE_THRESHOLD) {
	    // Replace with your timing mechanism

	    /*if (buttonState == SET) {
	    	
	    	
	        if (buttonStatus == BUTTON_STATE_IDLE) {
	            //buttonStatus = BUTTON_STATE_SINGLE;
	            pressCount = 1;
	        } 
	        else if (buttonStatus == BUTTON_STATE_SINGLE) {
	            // Check for a double press
	            if (currentTime - lastPressTime < LONG_PRESS_DELAY_MS) {
	               // buttonStatus = BUTTON_STATE_DOUBLE;
	                pressCount = 2;
	            }
	        }

//	        lastPressTime = currentTime;
//	   
//	}
	
*/	
	
	
	
	
	
	
	
	
